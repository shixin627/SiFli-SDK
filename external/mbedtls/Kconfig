
config PKG_SIFLI_MBEDTLS_BOOT
    bool "Seleted MBedTLS functions for boot"
    depends on !PKG_USING_MBEDTLS
    default n
config PKG_USING_SM
    depends on PKG_SIFLI_MBEDTLS_BOOT
    bool "Selected SM2/SM3"
    default n