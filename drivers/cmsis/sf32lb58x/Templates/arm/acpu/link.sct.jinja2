; NOTE:
; - This file is rendered by SiFli build system (ptab v3, Jinja2-only).
; - Do NOT add C preprocessor directives here (#include/#ifdef/#ifndef...).
; *************************************************************
; *** Scatter-Loading Description File generated by uVision ***
; *************************************************************

LR_IROM1 {{ hex32(defines['__ROM_BASE']) }} {{ hex32(defines['__ROM_SIZE']) }}  {    ; load region size_region
  ER_IROM1 {{ hex32(defines['__ROM_BASE']) }} {{ hex32(defines['__ROM_SIZE']) }}  {  ; load address = execution address
   *.o (RESET, +First)
   *(InRoot$$Sections)
   .ANY (+RO)
  }
  RW_IRAM1 AlignExpr({{ hex32(defines['__RAM_BASE']) }}+ImageLength(ER_IROM1), 16) ALIGN 16 NOCOMPRESS  {  ; RW data
   .ANY (+RW +ZI)
  }
  ; Load Address must be equal to Exec Address
  ScatterAssert((LoadBase(RW_IRAM1) OR {{ hex32(defines['__RAM_BASE']) }}) == ImageBase(RW_IRAM1))
  ScatterAssert((ImageLength(ER_IROM1)+ImageLength(RW_IRAM1))<{{ hex32(defines['__ROM_SIZE']) }})
}


;LR_IROM2 ACPU_CODE_REGION2_START_ADDR ACPU_CODE_REGION2_SIZE  {    ; load region size_region
;  ER_IROM2 ACPU_CODE_REGION2_START_ADDR ACPU_CODE_REGION2_SIZE  {  ; load address = execution address
;    *.o (.text.HAL_PMU_EnableXTAL32)
;  }
;}
