# PhysioNet sleep-accel raw data (Walch 2019, ODC-BY) — large (~50 MB/subject),
# downloaded on demand by the validation scripts. Not committed.
data/

# Derived feature cache — rebuild with build_features.py, do not commit (800 KB)
features.npz
