/* Vendor source built as part of the SDK, not by external/mbedtls's own
   SConscript. The whole explanation is in ../SConscript. */
#include "../../../../../../../external/mbedtls/library/platform.c"
