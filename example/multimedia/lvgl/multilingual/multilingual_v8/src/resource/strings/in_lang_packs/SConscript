from building import *

# Default example path: use the xlsx workbook to generate builtin lang_pack.c/h.
lang_mode = 'xlsx'
lang_sources = Glob('xlsx/*.xlsx')

# JSON source mode example.
# Uncomment the two lines below and comment the xlsx lines above to switch the
# demo back to the classic json -> c/h builtin generation path.
# lang_mode = 'json'
# lang_sources = Glob('json/*.json')

Return('lang_mode', 'lang_sources')
